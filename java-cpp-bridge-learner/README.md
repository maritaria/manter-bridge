The jar file generated by the ant build needs to be in one directory with file **"run.sh"** to work.

Generating jar file:
- Run **"ant"** in the project directory and artifacts will be generated in **"out/"** folder